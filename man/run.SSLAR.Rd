% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SSLAR.R
\name{run.SSLAR}
\alias{run.SSLAR}
\title{SSLAR (Spatial Single-cell Labeling and Annotation using Reference) Function}
\usage{
run.SSLAR(
  sc_count,
  sc_label,
  st_count,
  st_position = NULL,
  maker_path = NULL,
  min_cont = 0.01,
  numiters = 100,
  nmf = "ssNMF",
  regr = "lar",
  regr_type = "nnls"
)
}
\arguments{
\item{sc_count}{A matrix or data frame containing single-cell RNA sequencing counts.
Rows represent cells, and columns represent genes.}

\item{sc_label}{A data frame containing cell labels. It should have one column named "subclass".}

\item{st_count}{A matrix or data frame containing spatial transcriptomics counts.
Rows represent spots, and columns represent genes.}

\item{st_position}{Optional. A matrix or data frame containing spatial coordinates of the spots.
If provided, it should have two columns: "x" and "y".}

\item{maker_path}{Optional. Path to a precomputed marker gene list. If not provided, marker genes will be identified from the scRNA-seq data.}

\item{min_cont}{Minimum proportion threshold for cell types. Cell types with proportions below this threshold will be set to zero.}

\item{numiters}{Number of iterations for the NMF algorithm.}

\item{nmf}{Method for non-negative matrix factorization (NMF). Default is "ssNMF".}

\item{regr}{Regression method for deconvolution. Default is "lar" (Least Angle Regression).}

\item{regr_type}{Type of regression for deconvolution. Default is "nnls" (Non-negative Least Squares).}
}
\value{
A matrix or data frame containing the inferred cell type proportions for each spot.
}
\description{
This function performs spatial single-cell labeling and annotation using a reference dataset.
It integrates single-cell RNA sequencing (scRNA-seq) data with spatial transcriptomics (ST) data
to infer cell type proportions in each spatial location (spot).
}
\examples{
# Example usage:
# sc_count, sc_label, st_count, and st_position are example datasets
result <- run.SSLAR(sc_count = sc_count,
                    sc_label = sc_label,
                    st_count = st_count,
                    st_position = st_position,
                    min_cont = 0.01)

}
